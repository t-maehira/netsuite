<%= content_tag(:h1, 'エラーページ') %>

<ul id="flash_wrapper">
<% flash.each do |key, value| %>
  <%= content_tag(:li, value, class: "#{key}") %>
<% end %>
</ul>

<%= render :partial => 'shared/disclaimers' %>
